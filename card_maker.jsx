masterFile = File.openDialog("名刺のひな形を選んでください","*.indd");dirObj       = Folder.selectDialog("XMLが入ってるフォルダを選んでください");dirPath      = dirObj.fsName + "/";xmlFiles    = dirObj.getFiles("*.xml");for(i=0; i < xmlFiles.length; i++){    masterObj = app.open(masterFile);    masterObj.xmlImportPreferences.removeUnmatchedExisting = true;    xmlObj =  File(xmlFiles[i].fsName);    pdfObj =  File(dirPath + xmlFiles[i].name + ".pdf");    masterObj.importXML(xmlObj);    app.activeDocument.exportFile(ExportFormat.pdfType, pdfObj, false,"[PDF/X-1a:2001 (日本)]");    app.activeDocument.close(SaveOptions.no);}alert(i+"個のPDFを生成しました");